# Sanford Story Church — 웹사이트(web) · 주보(bulletin) · 앱(app) · PWA(app-web)
.DS_Store
.claude/
# 이 폴더의 다른 프로젝트·참고 자료 (저장소에 안 올림)
SunlgihtChurch/
error/
public/
/*.png
# 주보 PDF 는 make.sh 로 다시 만든다
bulletin/*.pdf
