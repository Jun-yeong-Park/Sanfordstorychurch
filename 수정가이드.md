# 수정 가이드 — 어디를 고치면 무엇이 바뀌나

모든 내용은 **`bulletin/data.js` 한 파일**에서 바꿉니다. 인쇄 주보 · QR 온라인 주보 · 앱이 전부 이 파일을 읽습니다.

## 매주 (3단계)
```bash
cd ~/develop/sanford/bulletin && open data.js        # 1. 내용 수정
./make.sh                                            # 2. PDF + 온라인 주보 파일 생성
cd .. && git add -A && git commit -m "주보 $(date +%F)" && git push   # 3. 배포 (QR 페이지 20초 뒤 반영)
```
앱에도 반영하려면 (Supabase 연결 후) `cd app && npm run push-bulletin` 한 줄 추가. 연결 전엔 앱은 빌드 시점 주보를 보여줍니다.

## data.js 항목별
| 바꿀 것 | 위치 | 메모 |
|---|---|---|
| 날짜 · 호수 · 예배시간 | `issue` | `dateISO` 는 PDF 파일명, `label` 은 표지 상단 (LAUNCH SUNDAY → SUNDAY WORSHIP) |
| 설교 제목 · 본문 · 설교자 | `sermon` | `scripture` 형식: `고린도후서 3:1–6` (앱·QR 본문 팝업이 이걸 읽음) |
| 오늘의 말씀 (P2 하단 박스) | `sermon.excerpt`, `excerptRef` | |
| 설교 영상 · 오디오 | `sermon.video`, `sermon.audio` | 앱·QR "설교 영상" 버튼. 비우면 안 보임 |
| 예배 순서 (4부) | `order[]` | 항목마다 `name` · `detail` · `by`. 부 제목/설명은 `name` · `sub` |
| 이번 주 찬양 | `order[0].items[1].songs[]` | `{ title, artist, url }`. url 비우면 유튜브 검색으로 열림 |
| 결단 찬양 | `order[2].items[0].songs` | 같은 형식 |
| 교회 소식 | `news[]` | 순서대로 01, 02… 5개 넘으면 인쇄판 P4 가 밀림 |
| 모임 | `groups[]` | |
| 기도 제목 | `prayers[]` | |
| 헌금 보고 | `offering` | `amount` 는 숫자. 항목 통째로 지우면 섹션이 사라짐 |
| 다음 주 예고 | `nextWeek` | |
| 섬기는 분들 (직함) | `team[]` | `role` 이 직함 |
| 이번 주 섬김 | `thisWeek[]` | |
| 일정 (앱 교회 탭 · 캘린더 추가) | `events[]` | `date` YYYY-MM-DD, `time` HH:MM |
| 교회 정보 (주소 · 이메일 · 헌금 · 인스타) | `church` | **주소가 아직 "예배 장소 주소 입력"** |
| 영어 | 각 항목의 `…En` | 비우면 한글 그대로 |

## 인쇄 주보 레이아웃 (거의 안 건드림)
`bulletin/index.html` — 4면 접지. P3 말씀 노트 3단 질문 문구는 여기 `pageNotes()` 와 앱 `app/lib/store.ts NOTE_STEPS` 두 곳.

## QR 온라인 주보
`web/bulletin/index.html` — 화면 구성·색. 내용은 data.js. 주소 `sanfordstorychurch.com/bulletin` 은 고정 (QR 재인쇄 불필요).

## 앱
| 바꿀 것 | 위치 |
|---|---|
| 색 · 폰트 | `app/lib/theme.ts` |
| 화면 문구 (한/영) | `app/lib/i18n.tsx` |
| 주보 화면 구성 | `app/components/BulletinView.tsx` |
| 말씀 노트 3단 질문 | `app/lib/store.ts` NOTE_STEPS |
| 탭 이름 · 아이콘 | `app/app/(tabs)/_layout.tsx`, `app/components/icons.tsx` |
| 교회 탭 (안내 · 계정 · 언어) | `app/app/(tabs)/church.tsx` |
| 앱 이름 · 버전 · 아이콘 | `app/app.json` (`version` 올리면 재심사 필요) |

앱 코드 수정 배포: 심사 없이 `cd app && npx eas-cli update --channel production --message "…"`. 새 네이티브 패키지·버전 변경만 `eas build` + `eas submit`.

## 웹사이트
`web/` — `index.html`(한) · `en/index.html`(영). push 하면 Netlify 자동 배포. 정책 페이지 `web/app/privacy.html` · `terms.html`.
